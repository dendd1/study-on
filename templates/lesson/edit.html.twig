{% extends 'base.html.twig' %}

{% block title %}Редакитрование урока '{{ lesson.name }}'/{{ lesson.course.name }}/StudyOn{% endblock %}

{% block body %}
    <div class="row mx-5 mt-3 justify-content-center">
        <h1 class="text-center mb-5">Редактирование урока</h1>


        {{ include('lesson/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="d-flex justify-content-center ">
            <a class="btn btn-outline-primary mt-3 fs-5 me-4" style="width: 200px"
               href="{{ path('app_lesson_show', {'id': lesson.id}) }}">назад к уроку</a>

            {{ include('lesson/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
